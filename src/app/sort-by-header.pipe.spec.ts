import { SortByHeaderPipe } from './sort-by-header.pipe';

describe('SortByHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
